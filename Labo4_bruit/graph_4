import numpy as np
import matplotlib.pyplot as plt
import pandas as pd
import os

dossier_script = os.path.dirname(os.path.abspath(__file__))
fichier_csv = os.path.join(dossier_script, "Values.1mode.xlsx")


#exporter les données
exp = pd.read_excel(fichier_csv, sheet_name=0)
xd = exp["angle"].to_numpy()
yd = exp["intensité"].to_numpy()


# graphique
plt.figure()

plt.plot(x, y1, linestyle='-', linewidth=1, label='', color="#898D8D")


plt.xlabel("Angle (°)", fontsize=20)
plt.ylabel("Intensité normalisée", fontsize=20)
plt.xticks(fontsize=18)
plt.yticks(fontsize=18)
plt.legend(fontsize=16)
plt.show()
